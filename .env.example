# Public site origin used for canonical URLs, OG images, sitemap, robots.
NEXT_PUBLIC_SITE_URL=https://learn.zerwiz.org

# SQLite database file (absolute path recommended in production).
DATABASE_URL=file:./db/custom.db

# --- Live playground: chat -------------------------------------------------
# OpenAI-compatible endpoint. Point at the llama-swap rail.
LLM_BASE_URL=http://127.0.0.1:8080/v1
LLM_API_KEY=
LLM_MODEL=qwen3.6-35b-a3b@q4_k_xl-mtp

# --- Live playground: image (optional) -------------------------------------
# Any OpenAI-compatible images endpoint. Blank IMAGE_API_KEY disables the tab.
IMAGE_BASE_URL=https://api.openai.com/v1
IMAGE_API_KEY=
IMAGE_MODEL=gpt-image-1

# --- Live playground: search (optional) ------------------------------------
# SEARCH_PROVIDER = searxng | brave | tavily | custom. Blank disables the tab.
SEARCH_PROVIDER=
SEARCH_API_URL=
SEARCH_API_KEY=
